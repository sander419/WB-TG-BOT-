# Дорожная карта

Порядок выбран так, чтобы каждый этап заканчивался проверяемым результатом,
а не «половиной подсистемы». Оценки — в рабочих днях одного разработчика.

## Этап 0. Каркас — сделано (2026-09-05)

Конфиг с валидацией, таксономия ошибок, логи с маскированием секретов,
шифрование токенов, HTTP-клиент с ретраями, лимитер, схема БД + миграция,
контракт коннекторов, реестр площадок, Telegram-бот на grammY, i18n (ru/en),
docker-compose, CI.

Плюс вторым заходом: тесты ядра (`npm test`, 60 проверок), разбор ответов WB
через zod, нормализация в общие типы, репозитории с идемпотентным upsert,
очередь `sync_jobs` на `SKIP LOCKED`, воркер синхронизации, роуты подключения
магазина.

Проверено: `npm run lint`, `npm test`, `npm run build` проходят; сервер
поднимается, `/api/platform/health` отвечает.
**Не проверено:** ни одного запроса к WB и к Postgres не выполнялось.

## Этап 1. Первый запуск с живой базой — сделано (2026-09-05)

- [x] PostgreSQL 17 поставлен нативно через `winget`, роль и база `commerceos` заведены
- [x] `SECRETS_ENCRYPTION_KEY` и `DATABASE_URL` в `.env`
- [x] `npm run db:migrate` — обе миграции легли, 16 таблиц и индексы на месте
- [x] `npm run smoke:db` — сквозной прогон слоя данных проходит
- [x] CI поднимает Postgres и гоняет тот же прогон

Прогон нашёл два дефекта, которые не ловились типами и модульными тестами:
разбор — `docs/ARCHITECTURE.md`, раздел «Что нашлось при первом запуске».

⚠️ У суперпользователя `postgres` пароль по умолчанию от установщика.
Локально терпимо, наружу порт не выставлять.

## Этап 2. Чтение из Wildberries — 2–4 дня

Код чтения уже написан, но не проверен на боевом токене. Задача этапа —
подтвердить или поправить его по факту. Грабли — `docs/INTEGRATION-WILDBERRIES.md`.

- [ ] Получить боевой токен, прогнать `npm run check:connection`
- [ ] Сверить пути в `endpoints.ts` с документацией, снять `verified: false`
- [ ] Сверить формы ответов в `schemas.ts` — при расхождении `parseWbResponse`
      падает с именем поля, править по сообщению
- [ ] `POST /api/platform/stores` — подключить магазин, токен уедет в БД зашифрованным
- [ ] Прогнать синхронизацию по модулям, сверить числа с личным кабинетом
- [ ] FBS-остатки отдельным источником, если магазин работает по FBS
- [ ] Позиции в поиске (`getSearchPositions`) — отчёт заказывается асинхронно
- [ ] Расписание синхронизации (cron или интервал в воркере)

**Готово, когда:** сутки синхронизации на боевом токене без ошибок, числа в БД
сходятся с личным кабинетом продавца.

## Этап 3. Реальные данные вместо демо — частично сделано

Расчёты, команды бота и слой данных написаны и проверены на живой БД.
Осталось перевести на неё веб-интерфейс и убрать демо.

- [x] Расчёт показателей: выручка и сравнение периодов, скорость продаж,
      запас дней, кто просел — **чистые функции**, счёт не доверен модели
- [x] Выборки из БД: позиции заказов, последние остатки, свежесть данных, отзывы
- [x] Сводка за сутки и отчёт по остаткам как сервис
- [x] Команды бота `/digest`, `/sales`, `/stocks`, `/stores`, `/sync` на данных из БД
- [x] Привязка магазина к Telegram через одноразовый код
- [x] Планировщик: воркер сам ставит синхронизацию по `SYNC_INTERVAL_MINUTES`
- [x] `/problems` — детерминированная диагностика просадки: нехватка товара,
      рост фактической цены, свежий негатив, всплеск отмен, системное падение.
      Гипотезы без источника (позиции, цены конкурентов) не выдумываются,
      а перечисляются как непроверенные
- [x] Алерты: out-of-stock, критический запас, просадка выручки, негативный
      отзыв, сломанная синхронизация — с подавлением повторов на 12 часов
- [x] `/reviews` — лента неотвеченных отзывов, худшие сверху, с черновиками
- [ ] Алерты по позициям в поиске и демпингу конкурентов (нет источника)
- [ ] Заменить захардкоженные ответы в `server.ts` на запросы к БД
- [ ] `USE_MOCK_DATA=false`, фронтенд ходит в реальные эндпоинты
- [ ] Убрать демо-эндпоинты и `src/data/mockStore.ts`
- [ ] Redis + общие лимитеры, если инстансов больше одного

**Готово, когда:** продавец видит в боте свои настоящие цифры за сутки.

## Этап 4. Запись в маркетплейс — 2–3 дня

- [ ] `action_audit` пишется до вызова API
- [ ] Подтверждение оператора в интерфейсе и в боте
- [ ] `updatePrices` с опросом статуса асинхронной задачи
- [ ] `updateStocks` (FBS)
- [ ] `replyToReview`
- [ ] Откат по снимку «до»

**Готово, когда:** цена меняется из бота с подтверждением, в аудите видно кто,
что и когда, и изменение откатывается одной командой.

## Этап 5. Ozon — 2–3 дня

Проверка контракта коннекторов на второй площадке. Если `MarketplaceConnector`
спроектирован верно, ни бот, ни UI не меняются. Если менять придётся — это
сигнал, что абстракция протекает; чинить сейчас, а не на пятой площадке.

- [ ] `Client-Id` + `Api-Key` в `credentials.extra`
- [ ] Чтение: товары, остатки, заказы, отзывы
- [ ] Мультивалютность на реальных данных

## Этап 6. Международный контур — оценка после этапа 5

- [ ] Shopify (GraphQL, бюджет стоимости запроса вместо счётчика)
- [ ] Китайские площадки: 1688, Taobao, JD — **сначала решить**, ложатся ли они
      в `MarketplaceConnector` вообще: это закупка, а не продажа, и домен у них
      другой (фабрики, MOQ, партии)
- [ ] Локали за пределами ru/en, валюты CNY/USD/EUR
- [ ] Требования к данным по юрисдикциям

## Этап 7. Мультиарендность и вход — сделано (2026-09-06)

- [x] Аутентификация: регистрация, вход, выход, смена пароля
- [x] Сессии в таблице (отзываемые), хеш токена вместо токена
- [x] Пароли на scrypt с версионированными параметрами
- [x] Роли из `memberships` (owner / admin / operator / viewer)
- [x] Организация берётся из сессии; роуты магазинов включены в production
- [x] Ограничение частоты на входе и регистрации
- [ ] Приглашения в существующую организацию
- [ ] Восстановление пароля (нужна отправка почты)

## Этап 8. Эксплуатация — параллельно с 3–4

- [x] Тесты нормализации ответов коннекторов (там ошибки тихие и дорогие)
- [x] Интеграционные тесты репозиториев на поднятом Postgres (34 проверки)
- [ ] Метрики: свежесть данных, ошибки коннекторов, использование лимитов
- [ ] Алертинг на упавшую синхронизацию
- [ ] Резервные копии БД
- [x] Деплой: `Dockerfile`, `docker-compose.prod.yml`, миграции при старте контейнера
- [x] Ротация ключа шифрования (`npm run rotate:key`)
- [ ] Поставить на VPS и проверить выкатку вживую (Docker на машине разработки нет)

## Что решить до этапа 2

1. **Одна страна или сразу несколько.** От этого зависит, нужны ли юридические
   поля в схеме (НДС, режим налогообложения) уже сейчас.
2. **Размерные линейки WB.** Если магазин торгует размерами, нужна сущность
   варианта товара — это правка схемы, дешевле сделать до наполнения данными.
3. **Кто хозяин цены.** Меняет её только человек через подтверждение, или
   правило может менять автоматически в заданном коридоре. Ответ определяет
   объём этапа 4.
